store-dir=/tmp/.pnpm-store
# virtual-store-dir intentionally omitted: use the pnpm default (node_modules/.pnpm).
# A previous hardcoded `/tmp/solana-clawd/.pnpm` path from another machine left
# node_modules/ references dangling on clones — don't reintroduce a host-specific path here.
