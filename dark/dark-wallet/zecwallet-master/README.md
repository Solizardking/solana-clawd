# Zecwallet Master

The source project carried a vendored `zecwallet-master` tree. The new Dark
Wallet workspace does not rely on those binaries directly, so this folder is
kept as a compatibility placeholder rather than a full vendor copy.

