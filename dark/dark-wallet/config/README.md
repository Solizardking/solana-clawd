# Config

This folder is reserved for runtime or environment-specific wallet settings.

The new Dark Wallet shell currently keeps its active runtime state in the
browser and does not require local config files yet.

