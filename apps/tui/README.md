# tui

Terminal user interface source for the Clawd runtime.

The TUI reads runtime configuration from environment variables and local state. Do not hard-code API keys or wallet secrets in this tree.
