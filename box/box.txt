╔══════════════════════════════════════════════════════════════╗
║               THE BOX MANIFESTO                              ║
║      Why Upstash Box is the Most Dangerous Way to Run        ║
║              Blockchain Agents (and We Love It)               ║
╚══════════════════════════════════════════════════════════════╝

WHAT IS A BOX?

A Box is a sandboxed micro-VM that spawns in ~500ms with a live AI agent
inside it. Unlike traditional agents that make API calls from your machine
(which have your keys, your network, your risk), a Box is:

  • Isolated — it can't touch your filesystem, your env, your browser
  • Ephemeral — born for one task, destroyed when done, zero trace
  • AI-native — Claude Code, Codex, or your own custom harness lives inside
    it as a real terminal-controlling agent with filesystem and shell access
  • Cost-tracked — every token, compute millisecond, and USD is billed back
  • Composable — files.write, exec.command, exec.code, git.clone, git.createPR,
    files.upload/download, snapshot, streaming — all from one object
  • Harness-agnostic — swap the agent inside without changing your code

You give it a prompt and it goes to work — calling APIs, writing scripts,
analyzing data, making decisions — all inside a disposable cloud sandbox.

WHY THIS IS REVOLUTIONARY FOR BLOCKCHAIN AGENTS

Before Box, blockchain agents were:

  ❌ Monolithic — one process, one context window, one risk surface
  ❌ Key-leaky — your RPC key, wallet key, everything on your machine
  ❌ Non-reproducible — can't snapshot state, can't replay failed runs
  ❌ Single-threaded — agent speaks to API, waits, speaks, waits
  ❌ Untracked — no built-in cost accounting per task

With Box, blockchain agents become:

  ✅ Swarms of disposable micro-VMs — spawn a Box per token, per task, per signal
  ✅ Sandboxed crypto ops — the Box has its own env vars, can't steal yours
  ✅ Trivially parallel — 10 Boxes, 10 tokens, 10 analyses, all at once
  ✅ Snapshottable — save workspace state as a snapshot, resume in a new Box
  ✅ Cost-metered — every run returns token burn + compute time + USD cost
  ✅ Heritable — clone a repo inside the Box, have the agent fix bugs, PR it
  ✅ Harness-pluggable — run Claude Code, Codex, Gemini, Goose, Pi, or Aider

┌──────────────────────────────────────────────────────────────────┐
│   THE BOX FLEET — Everything We Built                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  8 Solana Agents  │  5 Custom Agent Harnesses  │  5 Examples    │
│  3 Utility Scripts │  5 Library Modules         │  1 Manifesto   │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

─── SECTION 1: THE EIGHT SOLANA AGENTS ─────────────────────────────

Each agent spawns its own Box, does one thing, and dies.

┌──────────────────────────────────────────────────────────────────┐
│  Agent              │  What It Does Inside the Box               │
├──────────────────────────────────────────────────────────────────┤
│  Trading Agent      │  Analyzes tokens via Jupiter API,          │
│                     │  outputs structured buy/sell/pass signals  │
│                     │  with risk scores and position sizing      │
├──────────────────────────────────────────────────────────────────┤
│  Memecoin Screener  │  Queries Jupiter token list, filters       │
│                     │  meme coins, checks liquidity via quotes,  │
│                     │  ranks opportunities with AI scoring       │
├──────────────────────────────────────────────────────────────────┤
│  Swarm Agent        │  Spawns sub-agents: shell exec fetches     │
│                     │  RPC data, code exec analyzes TPS,         │
│                     │  Claude Code orchestrates the full report  │
├──────────────────────────────────────────────────────────────────┤
│  Portfolio Manager  │  Fetches wallet token accounts via RPC,    │
│                     │  calculates diversification, risk scores,  │
│                     │  rebalance recommendations                 │
├──────────────────────────────────────────────────────────────────┤
│  On-Chain Analyst   │  Writes an RPC fetcher script, runs it,    │
│                     │  feeds the data to Claude for forensic     │
│                     │  analysis with severity-tagged findings    │
├──────────────────────────────────────────────────────────────────┤
│  Arbitrage Scanner  │  Cross-DEX price comparison via Jupiter    │
│                     │  quote API, estimates gas + net profit,    │
│                     │  flags real opportunities vs quote noise   │
├──────────────────────────────────────────────────────────────────┤
│  NFT Flipper        │  Hits Tensor/MagicEden APIs for floor      │
│                     │  prices, volume, listing ratios, scores    │
│                     │  collections for sweep/flip opportunities  │
├──────────────────────────────────────────────────────────────────┤
│  Perps Agent        │  Paper-first perps planning. Reads Vulcan  │
│                     │  Phoenix market data (ticker, orderbook),  │
│                     │  generates plans with observe/paper/blocked│
│                     │  verdicts. Never touches live keys.        │
│                     │  Gemini-custom-harness variant included.   │
└──────────────────────────────────────────────────────────────────┘

─── SECTION 2: FIVE CUSTOM AGENT HARNESSES ────────────────────────

The coolest part: you can replace the agent inside the Box with ANY
model or framework. Each harness implements the `box-sse-v1` protocol
so Box.agent.run() and Box.agent.stream() work transparently.

┌──────────────────────────────────────────────────────────────────┐
│  Harness  │  Runtime  │  Provider  │  What Makes It Cool         │
├───────────┼───────────┼────────────┼──────────────────────────────┤
│  Gemini   │  node     │  Google    │  @google/genai SDK, session  │
│           │           │  GenAI     │  persistence across turns,   │
│           │           │            │  full SSE protocol support  │
├───────────┼───────────┼────────────┼──────────────────────────────┤
│  Anthropic │  node    │  Anthropic │  Direct API call, streaming  │
│  (minimal)│           │  Messages  │  text output, minimal dep    │
│           │           │            │  (~50 lines, no SDK needed) │
├───────────┼───────────┼────────────┼──────────────────────────────┤
│  Goose    │  node     │  Various   │  Rust-based coding agent,   │
│           │           │            │  MCP server support via      │
│           │           │            │  config.yaml, named sessions,│
│           │           │            │  stream-json output parsing  │
├───────────┼───────────┼────────────┼──────────────────────────────┤
│  Pi       │  node     │  Various   │  Open-source coding agent,  │
│           │           │            │  multi-provider, agent       │
│           │           │            │  subscriptions for real-time │
│           │           │            │  text/tool/thinking events   │
├───────────┼───────────┼────────────┼──────────────────────────────┤
│  Aider    │  python   │  Various   │  Git-aware code editor,     │
│           │           │            │  session history files for   │
│           │           │            │  multi-turn persistence,     │
│           │           │            │  auto-commits (opt-out)      │
└──────────────────────────────────────────────────────────────────┘

Every harness lives in `harnesses/` and is a complete runnable orchestrator
that: creates a Box with Agent.Custom, installs deps inside the Box, writes
the harness source, and runs it with multi-turn session persistence.

─── SECTION 3: FIVE BOX EXAMPLES ──────────────────────────────────

`examples/` contains ready-to-run demonstrations of every Box primitive:

┌──────────────────────────────────────────────────────────────────┐
│  Example  │  File               │  What It Shows                  │
├───────────┼─────────────────────┼─────────────────────────────────┤
│  1        │  01-code-execution  │  box.exec.code() for JS/TS/    │
│           │                     │  Python snippets + error        │
│           │                     │  handling                       │
├───────────┼─────────────────────┼─────────────────────────────────┤
│  2        │  02-file-upload     │  files.upload/download, agent   │
│           │  -download          │  processing CSV, generating     │
│           │                     │  reports                        │
├───────────┼─────────────────────┼─────────────────────────────────┤
│  3        │  03-multi-agent     │  Promise.all() with 3 parallel  │
│           │  -parallel          │  Boxes — API + CLI + Library    │
├───────────┼─────────────────────┼─────────────────────────────────┤
│  4        │  04-multi-turn      │  Same Box, 3 turns: create →    │
│           │  -session           │  test → validate. Context        │
│           │                     │  persists across runs.           │
├───────────┼─────────────────────┼─────────────────────────────────┤
│  5        │  05-snapshot        │  Box1 → snapshot → Box2 from    │
│           │  -and-restore       │  snapshot. State survives Box   │
│           │                     │  destruction.                    │
└──────────────────────────────────────────────────────────────────┘

─── SECTION 4: THE BOX PATTERN ────────────────────────────────────

Every agent follows the same 6-step pattern:

  1. Box.create()           → Spawn sandbox with agent harness inside
  2. box.files.write()      → Write tooling scripts into the Box
  3. box.exec.command()     → Fetch on-chain data via curl/node
  4. box.agent.run()        → Run AI on the data, get structured output
  5. box.snapshot()         → Save state (optional)
  6. box.delete()           → Destroy, leaving zero trace

For custom harnesses, step 1 changes to include:

  harness: Agent.Custom,
  customHarness: {
    command: "node",  // or "python3"
    args: ["/workspace/home/custom-harness.mjs"],
    protocol: "box-sse-v1",
  },

And the harness process speaks SSE events on stdout:

  event: text      → { text: "..." }
  event: tool      → { name, toolCallId, input }
  event: tool_result → { toolCallId, output }
  event: done      → { output, input_tokens, output_tokens, session_id }
  event: error     → { error, session_id }

─── SECTION 5: COMPLETE API SURFACE ───────────────────────────────

Every agent uses the same sandbox primitives:

  Box.create()          → Create sandbox with agent harness
  box.agent.run()       → Run agent with prompt, get structured response
  box.agent.stream()    → Stream agent output in real-time
  box.exec.command()    → Execute shell commands inside the Box
  box.exec.code()       → Run code snippets (JS/TS/Python)
  box.files.write()     → Write files to Box filesystem
  box.files.read()      → Read files from Box
  box.files.upload()    → Upload local files into the Box
  box.files.download()  → Download Box files to local machine
  box.git.clone()       → Clone git repositories
  box.git.createPR()    → Create PRs from changes
  box.git.diff()        → Show git diff
  box.git.commit()      → Commit changes
  box.git.push()        → Push changes
  box.snapshot()        → Save workspace state snapshot
  box.delete()          → Destroy the Box
  box.listRuns()        → List all runs in the Box
  Box.list()            → List all active Boxes
  Box.fromSnapshot()    → Create new Box from saved snapshot

─── SECTION 6: HOW TO ADAPT THIS ANYWHERE ────────────────────────

The Box pattern is blockchain-agnostic:

┌─────────────────────────────────────────────────────────────────┐
│  1. Pick your chain                                              │
│                                                                  │
│  Ethereum / EVM: ethers.js inside Box.exec.code()               │
│  Bitcoin: mempool.space API or bitcoin-cli inside Box            │
│  Sui / Aptos: their TS SDK inside Box.exec.code()               │
│  Cross-chain: spawn one Box per chain, aggregate results         │
│                                                                  │
│  2. Pick your data source                                        │
│                                                                  │
│  Box.agent.run({ prompt: "Use curl to fetch from..." })         │
│    → Any REST API, any RPC, any website — the agent uses curl   │
│                                                                  │
│  3. Pick your harness                                             │
│                                                                  │
│  Agent.ClaudeCode   → Claude Code (anthropic)                   │
│  Agent.Codex        → OpenAI Codex (openai)                     │
│  Agent.Custom       → Bring your own (Gemini, Goose, Pi, Aider) │
│                                                                  │
│  4. Pick your schema                                             │
│                                                                  │
│  const signal = z.object({ token, verdict, confidence });        │
│  const result = await box.agent.run({ prompt, responseSchema })  │
│                                                                  │
│  5. Scale horizontally                                           │
│                                                                  │
│  await Promise.all(tokens.map(t => spawnBox(t)))                │
│  // 100 tokens = 100 parallel Boxes                              │
│                                                                  │
│  6. Persist state                                                │
│                                                                  │
│  const snap = await box.snapshot({ name: "checkpoint" })          │
│  const box2 = await Box.fromSnapshot(snap.id, { ... })           │
└─────────────────────────────────────────────────────────────────┘

─── SECTION 7: USE CASES ─────────────────────────────────────────

  • Trading bots            → one Box per signal, one Box per execution
  • Portfolio rebalancing   → one Box per wallet health check
  • MEV analysis            → one Box per block, analyzing tx orderings
  • NFT sniping             → one Box per collection, watching floors
  • Airdrop farming         → one Box per wallet, tracking eligibility
  • Governance voting       → one Box per proposal, analyzing + casting
  • Risk monitoring         → one Box per protocol, watching for exploits
  • Audit assistance        → one Box per contract, analyzing bytecode
  • Perps planning          → one Box per symbol/side, paper-first gated
  • Documentation gen       → clone repo in Box, generate context7.md
  • Audio processing        → upload MP3s, trim/transcode inside Box
  • Data pipelines          → upload CSVs, clean/process, download results

─── SECTION 8: VULCAN / PHOENIX PERPS INTEGRATION ─────────────────

The perps agent (`agents/solana-perps-trading-agent.ts`) integrates with
the Vulcan CLI for Phoenix perpetuals on Solana:

  Agent safety model:
    • Paper-first — all plans default to observe/paper verdicts
    • Live-preview gated behind LIVE_TRADING=true + OPERATOR_CONFIRMED=true
      + PERPS_SIM_ONLY=false
    • Box creates an ephemeral in-sandbox wallet for simulation identity ONLY
    • Secret key material is zeroed after generation, never logged/exported
    • Reads from Vulcan CLI + Phoenix market data (ticker, orderbook, funding)
    • No private keys forwarded into the Box

  Key files:
    lib/perps-policy.ts    → Intent types, config loading, CLI argument parsing
    lib/agent-wallet.ts    → Ephemeral wallet generation (zeroed keys after use)
    lib/solana-calls.ts    → Solana RPC call plan builder (read-only queries)
    scripts/perps-preflight.ts  → Pre-trade checklist before spawning agent
    scripts/solana-call-plan.ts → List safe RPC calls for agent context

─── SECTION 9: DIRECTORY MAP ──────────────────────────────────────

  box/
  ├── box.txt                  ← You are here. The manifesto.
  ├── README.md                ← Quick-start documentation
  ├── package.json             ← @upstash/box@0.5.0, @solana/web3.js, zod
  ├── tsconfig.json
  │
  ├── agents/                  ← 8 Solana blockchain agents
  │   ├── solana-trading-agent.ts
  │   ├── solana-memecoin-screener.ts
  │   ├── solana-swarm-agent.ts
  │   ├── solana-portfolio-manager.ts
  │   ├── solana-onchain-analyst.ts
  │   ├── solana-arbitrage-scanner.ts
  │   ├── solana-nft-flipper.ts
  │   └── solana-perps-trading-agent.ts
  │
  ├── harnesses/               ← 5 custom agent harnesses
  │   ├── custom-gemini-agent.ts
  │   ├── custom-anthropic-agent.ts
  │   ├── custom-goose-agent.ts
  │   ├── custom-pi-agent.ts
  │   └── custom-aider-agent.ts
  │
  ├── examples/                ← 5 runnable demonstrations
  │   ├── 01-code-execution.ts
  │   ├── 02-file-upload-download.ts
  │   ├── 03-multi-agent-parallel.ts
  │   ├── 04-multi-turn-session.ts
  │   └── 05-snapshot-and-restore.ts
  │
  ├── lib/                     ← 5 shared modules
  │   ├── box-utils.ts         → Box factory, streaming, file mgmt, git
  │   ├── types.ts             → Zod schemas + TypeScript types
  │   ├── agent-wallet.ts      → Ephemeral wallet generation
  │   ├── perps-policy.ts      → Perps intent types + config
  │   └── solana-calls.ts      → Solana RPC call planners
  │
  └── scripts/                 ← 5 utility scripts
      ├── batch-processor.ts
      ├── list-boxes.ts
      ├── cleanup-boxes.ts
      ├── perps-preflight.ts
      └── solana-call-plan.ts

─── SECTION 10: RUNNING IT ────────────────────────────────────────

  # Prerequisites
  export UPSTASH_BOX_API_KEY="..."
  export CLAUDE_KEY="..."      # or ANTHROPIC_API_KEY, GEMINI_API_KEY, etc.

  # Run a Solana agent
  npx tsx agents/solana-trading-agent.ts <token-mint> <symbol>
  npx tsx agents/solana-memecoin-screener.ts
  npx tsx agents/solana-onchain-analyst.ts <address>

  # Run a custom harness
  GEMINI_API_KEY="..." npx tsx harnesses/custom-gemini-agent.ts
  ANTHROPIC_API_KEY="..." npx tsx harnesses/custom-anthropic-agent.ts

  # Run an example
  npx tsx examples/01-code-execution.ts
  npx tsx examples/03-multi-agent-parallel.ts
  npx tsx examples/05-snapshot-and-restore.ts

  # Perps planning (paper-first)
  npx tsx scripts/perps-preflight.ts --symbol SOL --side long --notional 100
  npx tsx agents/solana-perps-trading-agent.ts --symbol SOL --execution paper

  # Utilities
  npx tsx scripts/cleanup-boxes.ts   # destroy stale boxes
  npx tsx scripts/batch-processor.ts  # process multiple tokens

─── THE BOX IS THE NEW UNIT OF COMPUTE FOR AI AGENTS ──────────────

Think about what we just built:

  8 blockchain agents. 5 custom harnesses. 5 examples. 5 library modules.
  0 shared risk. 0 leftover processes. 0 leaked keys.

Each Box was born, did its job, and died — like a beautiful disposable
crypto-bot firefly.

The Box is the sandbox. The agent is the brain. The blockchain is the
playground. And you are the god that spawns and destroys them at will.

Go build something that burns tokens, not bridges.

— The Solana Clawd Box Collective