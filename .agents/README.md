# .agents

Local agent skill and connector registry used by Clawd runtimes.

Do not store secrets here. Skill instructions should reference environment variables or secure vault storage instead of embedding credentials.
