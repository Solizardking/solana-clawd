# Cleanup Audit

Generated: 2026-06-14

Scope: repository root

Constraint: Do not move or delete anything without explicit approval.

## Current Size

Total workspace size: `20G`

## Safest Rebuildable Cleanup Candidates

These paths are generated dependency installs, Rust/Solana build output, local tool state, or app build output. They should be recoverable by reinstalling dependencies or rebuilding, but deletion should still be approved path-by-path or by category.

| Path | Size | Why it is likely safe | Recovery |
| --- | ---: | --- | --- |
| `node_modules` | `4.0G` | Root JS dependency install; ignored by `.gitignore`. | `pnpm install` |
| `cheshire-terminal/node_modules` | `1.9G` | Nested JS dependency install; nested repo is clean. | `pnpm install` inside `cheshire-terminal` |
| `dynamic-bonding-curve-main/meteora-invent-main/node_modules` | `1.8G` | Nested JS dependency install. | install dependencies inside that package |
| `workers/browser-run/node_modules` | `243M` | Worker dependency install. | `pnpm --dir workers/browser-run install` |
| `staking/node_modules` | `189M` | Nested JS dependency install; nested repo is clean. | install dependencies inside `staking` |
| `dynamic-bonding-curve-main/node_modules` | `131M` | JS dependency install. | install dependencies inside `dynamic-bonding-curve-main` |
| `dynamic-bonding-curve-main/target` | `2.3G` | Rust/Anchor build output; ignored by `.gitignore`. | rebuild with Cargo/Anchor |
| `magicblock-gacha/target` | `2.1G` | Rust/Anchor build output; nested repo is clean. | rebuild with Cargo/Anchor |
| `staking/target` | `1.9G` | Rust/Anchor build output; nested repo is clean. | rebuild with Cargo/Anchor |
| `programs/cheshire-launchpad/target` | `384M` | Rust build output; ignored by `.gitignore`. | rebuild with Cargo/Anchor |
| `agent-minter/target` | `323M` | Rust build output; ignored by `.gitignore`. | rebuild with Cargo/Anchor |
| `m/agent-minter/target` | `286M` | Rust build output inside nested copy; nested repo is clean. | rebuild with Cargo/Anchor |
| `.mypy_cache` | `90M` | Python type-check cache; ignored by `.gitignore`. | regenerated by mypy |
| `.playwright-mcp` | `82M` | Local Playwright MCP console logs/state; ignored by `.gitignore`. | regenerated by browser tooling |
| `dist` | `12M` | Root build output; ignored by `.gitignore`. | `pnpm build` or `npm run build` |
| `.vercel` | `6.4M` | Local Vercel metadata; ignored by `.gitignore`. | `vercel pull` / relink project |
| `.vercel-static` | `6.4M` | Local Vercel/static output; ignored by `.gitignore`. | regenerated by Vercel tooling |

Approximate space in this category: `15.8G`.

## Larger Project Copies Requiring Human Decision

These are not cleanup targets unless explicitly approved. They contain source trees or full nested repositories.

| Path | Size | Notes |
| --- | ---: | --- |
| `cheshire-terminal` | `2.9G` | Appears to be another app copy with a clean `.git`; includes `1.9G` of `node_modules`. |
| `m` | `1.0G` | Appears to overlap with `cheshire-terminal`/root app; clean `.git`; includes copied app content and build artifacts. |
| `dynamic-bonding-curve-main` | `4.7G` | Source plus `target`, `node_modules`, and nested `meteora-invent-main/node_modules`. |
| `magicblock-gacha` | `2.7G` | Clean nested `.git`; mostly `target` and `.git` storage. |
| `staking` | `2.3G` | Clean nested `.git`; mostly `target`, `node_modules`, and `.git` storage. |

## Lockfile Notes

The root `package.json` declares `packageManager: pnpm@10.4.1`, and the root has both:

- `pnpm-lock.yaml`
- `package-lock.json`

The root `.gitignore` explicitly ignores `/package-lock.json`, so `package-lock.json` is probably a stale npm lockfile. It is not large (`1.4M`), but it is a cleanup candidate after confirming npm is not intentionally used for this root project.

## Dependency Usage Signals

Method: scanned root source/config files for static `import`, dynamic `import()`, and `require()` references while excluding `node_modules`, `target`, `dist`, and copied project directories such as `cheshire-terminal`, `m`, `dynamic-bonding-curve-main`, `staking`, `magicblock-gacha`, `better-auth-main`, `convex-helpers-main`, `TradingView-API-main`, `library`, and `caveman-agent`.

This is a conservative signal, not proof of safe removal. Packages can be used by CLIs, configs, type checking, generated code, peer dependencies, framework conventions, or runtime string lookups.

Root manifest dependency count: `184`

Root dependencies with at least one obvious source reference: `140`

Root dependencies with no obvious source reference in this scan: `44`

Likely expected false positives or tool/config dependencies:

- `@types/bn.js`
- `@types/canvas-confetti`
- `@types/compression`
- `@types/connect-pg-simple`
- `@types/cors`
- `@types/crypto-js`
- `@types/express`
- `@types/express-session`
- `@types/multer`
- `@types/node`
- `@types/passport`
- `@types/passport-local`
- `@types/pg`
- `@types/react`
- `@types/react-dom`
- `@types/three`
- `@types/uuid`
- `@types/ws`
- `autoprefixer`
- `esbuild`
- `postcss`
- `tsx`
- `typescript`
- `vercel`

Needs package-specific verification before removal:

- `@anthropic-ai/sdk`
- `@better-auth/core`
- `@better-auth/sso`
- `@emotion/react`
- `@emotion/styled`
- `@fal-ai/client`
- `@jridgewell/trace-mapping`
- `@mui/material`
- `@replit/object-storage`
- `borsh`
- `bufferutil`
- `connect-pg-simple`
- `memorystore`
- `passport`
- `passport-local`
- `stream-browserify`
- `zod-validation-error`

Do not remove any dependency from `package.json` based on this scan alone. The next safe step would be targeted verification for each package in the second list, including configs, scripts, generated code, and runtime string usage.

Additional targeted verification:

- `@crossmint/client-sdk-react-ui` is used by dynamic import in `client/src/components/CrossmintWalletProvider.tsx`.
- `@crossmint/wallets-sdk` is used by dynamic import in `client/src/lib/crossmintService.ts`.
- `@replit/vite-plugin-cartographer` is used by dynamic import in `vite.config.ts` for Replit dev environments.
- `@mui/material` and `@emotion/*` are referenced by Vite manual chunk rules, but no root source import was found in the targeted package-name search. They may be stale from an earlier MUI UI surface or needed indirectly by `@mui/x-charts`; verify with a clean build before removal.
- `@fal-ai/client` has no package import in root source; `server/routes/fal.ts` calls FAL over raw HTTP `fetch`, so this is a stronger removal candidate after checking lockfile/peer dependency impact.
- `@anthropic-ai/sdk` has no package import in root source; Anthropic model IDs and API key envs exist, but code appears to route through OpenAI-compatible clients or external gateways. Verify before removal.
- `connect-pg-simple`, `memorystore`, `passport`, and `passport-local` have no non-manifest references in the targeted scan. Current session middleware uses `express-session` plus `connect-redis`.
- `bufferutil` is likely an optional native performance dependency for `ws`, not an app import. Treat as optional/runtime dependency rather than obvious dead code.

## Scratch and Log Files

Additional small cleanup candidates found outside the large generated folders:

| Path | Size | Notes |
| --- | ---: | --- |
| `.playwright-mcp/console-2026-06-13T22-26-13-406Z.log` | `79M` | Single large Playwright MCP console log; most of `.playwright-mcp` size. |
| `dex-mobile-console.log` | `4.0K` | Root scratch log. |
| `caveman-agent/.google-agents-cli/run_server.log` | `4.0K` | Local tool run log. |
| `solanastreaming/.DS_Store` | `16K` | macOS metadata. |
| `solanastreaming/googlevoice-main/.DS_Store` | `8.0K` | macOS metadata. |
| `dynamic-bonding-curve-main/.anchor/program-logs/dbcij3LWUppWqq96dh6gJWwBifmcGfLSB5D4DuSMaqN.dynamic_bonding_curve.log` | `4.0K` | Anchor program log. |

These are all deletion candidates after approval. Only the Playwright MCP log materially affects disk usage.

## Duplicate Copy Signals

`cheshire-terminal` and `m` appear to be near-duplicate app copies after excluding generated folders:

- Shared file paths: `11,137`
- Paths only in `cheshire-terminal`: `2`
- Paths only in `m`: `0`
- At least one shared path differs by content: `scripts/pay.sh`

This makes `m` a strong duplicate candidate, but not a deletion target without explicit approval. It is a clean nested git repository and may still be useful as a checkpoint or alternate copy.

`cheshire-terminal` and the workspace root are not identical:

- Shared file paths after excluding large generated/copied directories: `10,864`
- Paths only in `cheshire-terminal`: `275`
- Paths only in root: `32,818`
- The raw diff shows many root-only additions and many changed app files, routes, components, scripts, server modules, and configs.

Do not delete `cheshire-terminal` as a simple duplicate of root. It needs a canonical-source decision first.

## Approval Sets

Recommended first pass:

1. Delete only dependency installs: all listed `node_modules` paths.
2. Delete only Rust/Anchor build outputs: all listed `target` paths.
3. Delete only local tool/build caches: `.mypy_cache`, `.playwright-mcp`, `dist`, `.vercel`, `.vercel-static`.
4. Delete all safest rebuildable candidates above.

Do not delete the larger project-copy directories unless the canonical source tree has been confirmed.

## Dry-Run Cleanup Helper

A helper script exists at `scripts/cleanup-audit.mjs`.

It defaults to dry-run mode and only prints audited paths:

```sh
node scripts/cleanup-audit.mjs generated
```

Available sets:

- `deps`: dependency install folders only.
- `targets`: Rust/Anchor build outputs only.
- `caches`: local tool/build caches only.
- `logs`: scratch/log files only.
- `generated`: `deps` + `targets` + `caches`.
- `all`: `generated` + `logs`.

Deletion requires the explicit `--apply` flag and should only be run after approval:

```sh
node scripts/cleanup-audit.mjs --apply generated
```
